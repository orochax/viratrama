# RLS e segurança

RLS fica ativa em todas as tabelas expostas. Jogadores só leem a própria sessão; mensagens privadas filtram destinatário; respostas futuras não são públicas; service role é somente servidor.
