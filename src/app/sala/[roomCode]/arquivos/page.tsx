import { GameShell } from "@/components/game/GameShell"; import { FileText, Lock } from "lucide-react";
export default function Arquivos({params}:{params:{roomCode:string}}){return <GameShell roomCode={params.roomCode}><p className="eyebrow">Dossiê da equipe</p><h1 className="serif mt-4 text-6xl">Arquivos desbloqueados.</h1><div className="mt-8 space-y-3">{[["DOC-01","Convite do Instituto Voss","Disponível"],["DOC-02","Planta da mansão","Disponível"],["DOC-03","Registro de leilão","Parcial"],["DOC-04","Protocolo Janus","Bloqueado"]].map(([code,title,status])=><div className="panel flex items-center gap-4 p-5" key={code}><FileText className="text-[#c7a96b]"/><div><p className="eyebrow">{code}</p><p className="mt-1">{title}</p></div><span className="ml-auto text-xs text-[#99a1ae]">{status === "Bloqueado"&&<Lock className="inline mr-1" size={13}/>} {status}</span></div>)}</div></GameShell>}
