import { GameShell } from "@/components/game/GameShell"; import { Package, Check } from "lucide-react";
export default function Inventario({params}:{params:{roomCode:string}}){return <GameShell roomCode={params.roomCode}><p className="eyebrow">Inventário coletivo</p><h1 className="serif mt-4 text-6xl">O que a equipe carrega.</h1><div className="mt-8 grid gap-4 md:grid-cols-2">{[["INV-01","Cartão de acesso","Uso único · encontrado no Envelope 01",true],["INV-02","Lente azul","Revela tinta de segurança no papel",true],["INV-03","Chave Atlas A","Sem confirmação de autenticidade",false],["INV-04","Comunicador","Canal com Vega",true]].map(([code,name,desc,owned])=><div className="panel flex gap-4 p-5" key={code as string}><span className="grid h-11 w-11 shrink-0 place-items-center border border-[#c7a96b]/30 text-[#c7a96b]"><Package size={17}/></span><div><p className="eyebrow">{code as string}</p><h2 className="serif mt-1 text-2xl">{name as string}</h2><p className="mt-1 text-sm text-[#99a1ae]">{desc as string}</p></div>{owned&&<Check className="ml-auto text-[#3e7b62]"/>}</div>)}</div></GameShell>}
