"use client";
import Link from "next/link";
import { Archive, Clock3, Home, Map, MessageSquare, Package, Pause, ShieldAlert } from "lucide-react";
import { useState } from "react";
export function GameShell({roomCode,children}:{roomCode:string;children:React.ReactNode}){const [paused,setPaused]=useState(false);return <div className="min-h-screen pb-24"><header className="border-b border-white/10 px-5 py-4"><div className="mx-auto flex max-w-6xl items-center justify-between"><Link href={`/sala/${roomCode}`} className="flex items-center gap-3"><span className="grid h-8 w-8 place-items-center border border-[#c7a96b]/50 text-[#c7a96b]"><ShieldAlert size={15}/></span><span className="text-xs tracking-[.18em]">OMN / {roomCode}</span></Link><div className="flex items-center gap-4 text-xs text-[#99a1ae]"><span className="flex items-center gap-2"><Clock3 size={14} className="text-[#c7a96b]"/>23:12</span><span className="hidden md:inline">ALERTA 0 / 5</span><button onClick={()=>setPaused(!paused)} className="button-ghost flex min-h-9 items-center gap-2 px-3"><Pause size={14}/>{paused?"Retomar":"Pausar"}</button></div></div></header>{paused&&<div className="border-b border-[#c7a96b]/30 bg-[#c7a96b]/10 px-5 py-3 text-center text-sm text-[#c7a96b]">Sessão pausada pelo anfitrião. O relógio narrativo está congelado.</div>}<main className="mx-auto max-w-6xl px-5 py-8">{children}</main><nav className="fixed bottom-0 left-0 right-0 z-10 border-t border-white/10 bg-[#0e1219]/95 px-2 py-3 backdrop-blur"><div className="mx-auto flex max-w-xl justify-around text-[10px] text-[#99a1ae]"><Nav href={`/sala/${roomCode}/jogo`} icon={<Home/>} label="Jogo"/><Nav href={`/sala/${roomCode}/mapa`} icon={<Map/>} label="Mapa"/><Nav href={`/sala/${roomCode}/inventario`} icon={<Package/>} label="Itens"/><Nav href={`/sala/${roomCode}/arquivos`} icon={<Archive/>} label="Arquivos"/><Nav href={`/sala/${roomCode}/mensagens`} icon={<MessageSquare/>} label="Mensagens"/></div></nav></div>}
function Nav({href,icon,label}:{href:string;icon:React.ReactNode;label:string}){return <Link href={href} className="flex flex-col items-center gap-1 hover:text-[#f4f1e8]">{icon}<span>{label}</span></Link>}
